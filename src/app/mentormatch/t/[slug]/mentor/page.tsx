// Placeholder — real mentor dashboard arrives in Fase 7. Exists so the route
// and post-login destination resolve and the guard chain is build-verified.
export default function MentorDashboardPage() {
  return <div>Mentor dashboard (placeholder — Fase 7)</div>;
}
