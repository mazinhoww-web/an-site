// Placeholder — real super admin panel arrives in Fase 9. Exists so the route
// and SUPER_ADMIN post-login destination resolve and the guard is build-verified.
export default function SuperAdminPage() {
  return <div>Super admin (placeholder — Fase 9)</div>;
}
